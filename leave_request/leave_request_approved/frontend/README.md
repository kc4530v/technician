# leave_request_approved

Reproduction of the **leave_request_approved** screen from `leave_request/leave_request_approved.pdf` (same structure as
`screen_chat`). Approved result screen with approver info and Back to My Requests. Brand purple `#6A4DBB`.

## Run
```bash
cd frontend && npm install && npx expo start   # press w for web
```
