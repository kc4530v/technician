// Test IDs for the Settings feature. See ./index.js for the recipe to add a
// new feature file. Keys are camelCase; values are kebab-case `settings-<element>`.

export const SETTINGS = {
	screen: 'settings-screen',
	backButton: 'settings-back-button',
	// row testIDs are built as `${rowPrefix}-${key}` e.g. 'settings-row-notifications'
	rowPrefix: 'settings-row',
	saveButton: 'settings-save-button',
};
